import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Convertitore',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
